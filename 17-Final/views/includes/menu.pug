.list-group.mb-3
       if categories.length > 0
              each category in categories
                     a(href=`/categories/${category._id}` class=category._id==selectedCategory?"list-group-item list-group-item-action active":"list-group-item list-group-item-action") #{category.name}