extends ../layouts/main-layout.pug

include ../mixins/create-product.pug

block content
    .row 
        .col-md-3 
           // include ../includes/menu.pug
        .col-md-9 
            .row
                if products.length > 0
                    each product in products
                        +createProduct(product)
                else
                    h1.mt-3.ml-3 Ürün Yok